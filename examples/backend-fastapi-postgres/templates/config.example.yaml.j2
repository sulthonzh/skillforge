# Example configuration for {{ skill.title }}.
# Copy to config.local.yaml and fill in values. Never commit secrets.
environment: "{{ skill.name }}"
# TODO: add environment variables and feature flags for your project.
