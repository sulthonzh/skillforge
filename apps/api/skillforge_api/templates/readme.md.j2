# {{ skill.title }}

{{ skill.description }}

- **Domain:** {{ skill.domain }}
- **Version:** `{{ skill.version }}`
- **Status:** `{{ skill.status }}`
- **Generated by:** SkillForge (`{{ ai.planner_model or 'mock' }}`)

## Install

This skill is already installed under the SkillForge skills directory if you used
`skillforge install`. To install manually into a ZCode-style skills folder, copy
this directory to your skills root and ensure `SKILL.md` is at the top level.

## Tools

{% for tool in tools -%}
- **{{ tool.name }}** _({{ tool.category }})_ — {{ tool.reason or "—" }}
{% endfor %}

## Workflow

{% for step in workflow -%}
{{ loop.index }}. {{ step }}
{% endfor %}

## Files

{% for f in outputs.required_files -%}
- `{{ f }}`
{% endfor %}

{% for d in outputs.required_directories -%}
- `{{ d }}/`
{% endfor %}

## Safety

Generated scripts in `scripts/` are reference examples only. SkillForge never
auto-executes them. Review any script before running.

---

_Edit this README to match your project. This file was generated by SkillForge._
