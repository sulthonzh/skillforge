# {{ skill.title }}

> SkillForge-generated skill. Edit freely. Status: **{{ skill.status }}** · Version `{{ skill.version }}`

## Purpose

{{ skill.description }}

This skill exists to give engineers a **specific, tool-driven** workflow for
{{ skill.domain | lower }} work — not a generic checklist. Every section below is
shaped by the chosen stack.

## When to Use

Use this skill when the user needs help with:

{% for prompt in example_prompts[:5] -%}
- {{ prompt }}
{% endfor %}
- Setting up a new {{ skill.domain | lower }} project with the tools below.
- Reviewing an existing setup against these best practices.

## When Not to Use

Do not use this skill when:

- The request is unrelated to {{ skill.domain | lower }}.
- The selected tools are not the stack the project actually uses.
- The user needs a different technology stack (regenerate a more specific skill).

## Tools and Stack

| Category | Tool | Purpose |
|---|---|---|
{% for tool in tools -%}
| {{ tool.category }} | **{{ tool.name }}** | {{ tool.reason or "—" }} |
{% endfor %}

## Architecture

Recommended patterns:

{% for pattern in architecture.patterns -%}
- {{ pattern }}
{% endfor %}

## Workflow

Follow this step-by-step workflow. Each step should map to concrete artifacts.

{% for step in workflow -%}
{{ loop.index }}. {{ step }}
{% endfor %}

## Best Practices

{% for item in best_practices -%}
- {{ item }}
{% endfor %}

## Output Standards

The skill should produce:

{% for item in output_standards -%}
- {{ item }}
{% endfor %}

## Example Prompts

Realistic prompts that should activate this skill:

{% for prompt in example_prompts -%}
- "{{ prompt }}"
{% endfor %}

## Example Outputs

Sample outputs generated by this skill:

{% for item in example_outputs -%}
- {{ item }}
{% endfor %}

## Safety

- Auto-execute scripts: **{{ safety.auto_execute_scripts }}**
- Require user confirmation before install: **{{ safety.require_user_confirmation_before_install }}**
- Allow network access: **{{ safety.allow_network_access }}**

Never run generated scripts automatically. Treat them as reference examples.

## Validation Checklist

- [ ] Tools are specific.
- [ ] Workflow is clear and references the chosen tools.
- [ ] Examples are realistic.
- [ ] Best practices are included.
- [ ] Output standards are defined.
- [ ] Required files exist: {% for f in outputs.required_files %}`{{ f }}`{% if not loop.last %}, {% endif %}{% endfor %}.

---

_Generated by SkillForge using model `{{ ai.planner_model or 'mock' }}`._
